export const metadata = { title:"echo-copilot", description:"Real-time conversational AI co-pilot built for a specific family member — Phase 0 prototype." };
export default function RootLayout({ children }) {
  return (<html lang="en"><body>{children}</body></html>);
}
